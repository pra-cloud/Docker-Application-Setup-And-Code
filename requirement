#### Command For Setup the Virtual Machine

# Install dependencies

1. install docker
systemctl install docker
systemctl enable docker
systemctl start docker

2. install httpd
syatemctl install httpd
systemctl enable httpd
systemctl start httpd

# Run for disable the selinux 

setenforce 0 

# inside suoders file give root and apache server permission

 vim /etc/sudoers

root    ALL=(ALL)       ALL

apache  ALL=(ALL)      NOPASSWD: ALL

#now put all code files and directories inside the cgi -bin server location

cd /var/www/cgi-bin/

# give permission for editing to all files run this command

chmod +x *
